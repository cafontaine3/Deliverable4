This program is similar to deliverable 2, but in the python language.

The program takes user input between 5 adn 15, then creates a random list of that length.

It then sums the elements and prints them to the console.